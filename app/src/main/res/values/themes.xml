<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A platform theme, not Theme.Material3.*: those live in the Material
      *View* library, which a Compose-only app has no reason to depend on.
      Colours come from MaterialTheme in Compose; this just hosts the activity.
    -->
    <style name="Theme.Recite" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
